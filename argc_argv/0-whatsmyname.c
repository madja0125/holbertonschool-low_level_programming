#include <stdio.h>

/**
 * main - starting point
 * @argv: argument vector
 * @argc: argument counter
 * Return: Integer
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
